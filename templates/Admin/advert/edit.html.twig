{% extends 'base.html.twig' %}

{% block title %}Edit Advert{% endblock %}

{% block body %}
    <h1>Edit Advert</h1>
    {# {{dump(advert)}}
    {{dump(form)}} #}

    {{ include('Admin/advert/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('advert_index') }}">back to list</a>

    {{ include('Admin/advert/_delete_form.html.twig') }}
{% endblock %}
